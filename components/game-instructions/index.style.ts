import { CSSProperties } from "react";

const contextButtonSize = "60px";

export const neonBoxStyle: CSSProperties = {
  width: "700px",
  marginTop: "20px",
  marginBottom: "20px",
  maxHeight: "25vh",
};

export const backButtonStyle: CSSProperties = {
  width: contextButtonSize,
  height: contextButtonSize,
};
