export enum WebSocketEvent {
  UPDATE_ROOM = "update-room",
  JOIN_ROOM = "join-room",
  JOIN_GAME = "join_game",
  GO_TO_ROOM_HOME = "go_to_room_home",
  SELECT_CATEGORY = "select_category",
  ROOM_DELETED = "room_deleted",
}
